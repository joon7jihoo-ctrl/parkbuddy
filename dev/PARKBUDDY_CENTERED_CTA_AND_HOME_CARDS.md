# ParkBuddy centered CTA and home card polish

## Goal

- Keep bottom fixed action buttons centered instead of stretched across the full viewport.
- Make the home recent round and notice cards fully clickable.
- Remove redundant labels such as "일정 보기" and "게시판" from the recent content cards.

## Verification

- Run `npm run verify`.
- Check that bottom fixed CTA groups are centered and no longer span the whole screen.
- Check `/` and confirm the recent round and recent notice cards are full-card links.
